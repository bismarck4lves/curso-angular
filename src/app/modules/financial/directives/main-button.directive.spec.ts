import { MainButtonDirective } from './main-button.directive';

describe('MainButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MainButtonDirective();
    expect(directive).toBeTruthy();
  });
});
